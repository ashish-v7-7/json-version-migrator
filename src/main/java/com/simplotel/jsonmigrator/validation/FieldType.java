package com.simplotel.jsonmigrator.validation;

/**
 * Expected data types for JSON field validation.
 */
public enum FieldType {
    STRING,
    NUMBER,
    BOOLEAN,
    OBJECT,
    ARRAY,
    ANY
}
